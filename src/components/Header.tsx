import React from "react";
import HyperText from "@/components/magicui/hyper-text";

const Header = () => {
  return (
    <header className="text-center mb-8">
      <HyperText text="TIPOTHEHAT" className="text-4xl font-bold mb-4" />
    </header>
  );
};

export default Header;
