import React from "react";
import HyperText from "@/components/magicui/hyper-text";

const Footer = () => {
  return (
    <footer className="text-center mb-8">
      <HyperText text="$degen" className="text-4xl font-bold mb-4" />
    </footer>
  );
};

export default Footer;
